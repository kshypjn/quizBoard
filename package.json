{
  "name": "quiz-scoreboard",
  "version": "1.0.0",
  "description": "A quiz scoreboard application with team management and scoring",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "client": "cd client && npm start",
    "client-install": "cd client && npm install",
    "build": "cd client && npm run build",
    "vercel-build": "cd client && npm install && npm run build",
    "heroku-postbuild": "npm run client-install && npm run build"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "socket.io": "^4.8.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "keywords": [
    "quiz",
    "scoreboard",
    "team",
    "scoring"
  ],
  "author": "Quiz Master",
  "license": "MIT"
}
